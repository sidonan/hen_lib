import {Button} from "./components/Button";

function App() {
  return (
    <div>
      <Button variant="bordered" >Confirn</Button>
      <Button>Cancel</Button>
    </div>
  );
}

export default App;
